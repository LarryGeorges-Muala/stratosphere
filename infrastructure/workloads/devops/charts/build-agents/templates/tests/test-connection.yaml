apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "build-agents.fullname" . }}-test-connection"
  namespace: {{ .Values.namespace | default "devops" }}
  labels:
    {{- include "build-agents.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "build-agents.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
